<!DOCTYPE html>
<html>
  <head>
    <title>HomeAlone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-slate-600 py-4 px-4 mb-8">
      <% if trainer_signed_in? %>
        <div class="flex flex-row-reverse gap-x-6">
          <span class="text-white font-bold hover:underline"><%= link_to "Sign Out", destroy_trainer_session_path, data: { turbo_method: :delete }%></span>
          <span class="text-white"><%= current_trainer.email %></span>
        </div>
      <% else %>
        <%= link_to "Sign In", new_trainer_session_path, class: "text-white" %>
      <% end %>
    </nav>
    <div class="container mx-auto">
      <% if flash[:notice] %>
        <div class="bg-blue-100 border border-blue-400 text-blue-700 px-4 py-3 rounded relative mb-4"><strong class="font-bold"><%= flash[:notice] %></strong></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
